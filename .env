# Sandbox Configuration
# URL for Cuckoo/CAPE Sandbox API
SANDBOX_API_URL=http://localhost:8090/tasks/create/file
# API Token if required (leave empty if not)
SANDBOX_API_TOKEN=

# Set to 'true' to use the internal mock sandbox (no external server required)
USE_MOCK_SANDBOX=true
